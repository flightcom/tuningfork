<ul class="nav navbar-nav submenu pull-right">
    <li>
    	<a href="" title="RàZ" ng-click="tmParams.filter({})" ng-class="{active: utils.keys(tmParams.filter()).length}">
            <span class="badge">{{tmParams.total()}}</span>
    	</a>
    </li>
</ul>
